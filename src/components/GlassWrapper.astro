---
interface Props {
  padding?: boolean;
}

const { padding = true } = Astro.props;
---

<div
  class={`bg-white/5 backdrop-blur-md border border-white/20 rounded-xl shadow-lg mt-5 ${padding ? 'p-6' : ''}`}
>
  <slot />
</div>
